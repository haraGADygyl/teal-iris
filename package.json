{
  "name": "teal-iris",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "apps/backend/**/*.{ts,js}": [
      "pnpm --filter backend lint"
    ],
    "apps/frontend/**/*.{ts,tsx,js,jsx}": [
      "pnpm --filter frontend lint"
    ]
  }
}
